singleNode: true

service:
  resources:
    requests:
      cpu: "50m"
      memory: "128Mi"
    limits:
      cpu: "200m"
      memory: "512Mi"

storageClass: "local-path"

data:
  resources:
    requests:
      storage: 1Gi
